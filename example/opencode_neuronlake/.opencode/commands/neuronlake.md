# NeuronLake

Use the local NeuronLake provider configured in `opencode.json`.

Suggested prompt:

```text
Inspect sample/buggy-counter.ts and suggest the smallest immutable fix. Do not edit files.
```
